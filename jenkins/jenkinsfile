pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            agent {
                docker {
                    image 'golang:1.25'
                    reuseNode true
                }
            }

            steps {
                dir('bugtracker-backend') {
                    sh 'go test -v ./..'
                }
            }
        }
    }
}